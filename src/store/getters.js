const getters = {
  selectedCategories: (state) => state.selectedCategory,
  month: (state) => state.month,
  startDate: (state) => state.startDate,
  endDate: (state) => state.endDate,
  today: (state) => state.today
}

export default getters